<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.kdt.gametrend.member.MemberDAO" >
 	<!-- 로그인 -->
 	<select id="loginMember" resultType="memberdto" parameterType="memberdto">
		select * from member where id=#{id} and password=#{password}
	</select>
	
	<!-- 회원가입 -->
	<insert id="joinMember" parameterType="memberdto">
		insert into member values(#{id}, #{password}, #{name}, #{birthDate}, #{gender}, #{phone}, #{image} #{lastLogin})
	</insert>
	
	<!-- 회원가입 아이디 중복 확인 -->
	<select id="checkId" resultType="String" parameterType="String">
		select id from member
		where id=#{id}
	</select>
	
	
	
	<!-- 로그인시 아이디와 비밀번호 확인 -->
	<!-- <select id="loginUser" resultType="memberdto">
		select * from member where id=#{id} and password=#{password}
	</select> -->
	
	<!-- 아이디 찾을 때 이름과 폰번호 조회 -->
	<!-- <select id="findId" resultType="memberdto">
		select * from member where name=#{name} and phone=#{phone}
	</select> -->
	
	<!-- 비밀번호 찾을 때 아이디와 이름 폰번호 조회 -->
<!-- 	<select id="findPassword" resultType="memberdto">
		select * from member where id=#{id} and name=#{name} and phone=#{phone}
	</select> -->
</mapper>
